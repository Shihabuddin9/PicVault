import SignUp from "@/component/shared/Login/Login";
import { Box } from "@mui/material";

const Login = () => {
    return (
        <Box>
            <SignUp />
        </Box>
    );
};

export default Login;