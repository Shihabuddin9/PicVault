import PrivateRoutes from "@/component/PrivateRoutes/PrivateRoutes";

const Profile = () => {
    return (

        <PrivateRoutes>
            <h1>profile</h1>
        </PrivateRoutes>

    );
};

export default Profile;

